#include "main.h"

/*
* add - adds two integers and returns results
*
* Return - Always 0
*
*/

int add(int, int)
{
    int mia, pat, result;
    reslut = mia + pat;
    return(result);
}