#include "main.h"

int print_last_digit(int)
{
    int n;
    int i;
    if(n >= 1)
    {
        i = n % 10;
        return(i);
    }
    else if (n <= -1)
    {
        i = n % 10;
        return(i);
    }
    else 
    return(0)
}