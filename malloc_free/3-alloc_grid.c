#include "main.h"
#include <stdlib.h>

int **alloc_grid(int width, int height)
{
    int width = 0;
    int height = 0;
    int 

    if (width <= 0 || height <= 0)
    {
        return (NULL); /* If width or height are zero or less then return NULL */
    }

    while (m < width) /*Loop through the width and set it to 0*/
    {   m = width;
        width++;
        m;
    }
    
}