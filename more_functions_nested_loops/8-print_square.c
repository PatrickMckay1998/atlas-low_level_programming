#include "main.h"

void print_square(int size)
{
    int m;
    if (size <= 0)
    {
        _putchar('\n');
    }
    for (m = 0; m < size; m++)
    {
        _putchar('#');
        _putchar('\n'):
    }
}