#include "main.h"
#include <stdlib.h>

int *arraY_range(int min, int max)
{   
    int arr_1;
    
    arr_1 = malloc(max - min + 1);

    if(min > max)
    {
        return (NULL);
    }
    
    m = 0;
    i = 0;

    while (i >= min && i <= max)
    {
        j[m] = i;
    }




}