#include <stdio.h>
#include <stdlib.h>
#include "dog.h"

dog_t *new_dog(char *name, float age, char *owner)
{
    *name = malloc(char);
    *owner = malloc(char);
}

/* get the length of name and owner and allocate space for the name and owner
create a copy of the name and owner into a new variable 
*/