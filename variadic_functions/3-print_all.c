#include <stdio.h>
#include <stdarg.h>

void print_all(const char * const format, ...)
{
    va_list args;

    va_start(args, format);

    nico = 0;

    while (nico < format)
    { 
        if(va_arg(args, format) = )
        nico++;
    }
}

/* Whiteboard this one tomorrow bro, great job and lets keep up the grind */